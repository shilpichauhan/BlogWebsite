<div class="row flex-lg-row-reverse align-items-center g-5 py-4 mb-4">
    <div class="col-12 col-lg-6">
        <img src="/img/girl.jpg" width="607" height="510" class="d-block mx-lg-auto img-fluid" loading="lazy"
            alt="Blog With Node.js">
    </div>

    <div class="col-12 col-lg-6">
        <h1 class="display-5 fw-bold mb-3">Huge collection of Blogs</h1>
        <p class="lead">
            Thoughts And Ideas Around The Things That Actually Matters. Became Creative And Innovative By Writting A Blogs That Express Your Views.
        </p>

        <div class="d-grid gap-2 d-md-flex justify-content-md-start">
            <a href="/explore-latest" class="btn btn-primary btn-dark btn-lg px-4 me-md-2">Explore Latest</a>
            <a href="/explore-random" class="btn btn-outline-secondary btn-lg px-4 me-md-2">Show Random</a>
        </div>

    </div>

</div>

<!-- category image -->
<div class="row row-cols-2 row-cols-lg-6 g-2 g-lg-3 py-4">
    <% if(categories!='' ){ %>
        <% categories.forEach(function(category,index){ %>
            <a href="/categories/<%= categories.name %>" class="col text-center category__link">
                <div class="category__img shadow">
                    <img src="/img/<%= category.image %>" alt="<%= category.name %>" loading="lazy">
                </div>
                <div class="pt-1"><%= category.name %></div>
            </a>
        <% }) %>
    <% } %>
    <a href="/categories" class="col text-center category__link">
         <div class="category__img shadow">
                 <img src="/img/arrow.jpg" alt="Dancing" loading="lazy">
        </div>
         <div class="pt-1">View All</div>
    </a>

</div>


<!-- category text -->

<!-- latest start  -->
<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Latest Blogs</h2>
        <a href="/explore-latest" class="ms-auto">View More</a>
    </div>

    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if(food.latest!=''){ %> 
            <% food.latest.forEach(function(blog,index){ %> 
        <a href="/blog/<%= blog._id %>" class="col text-center cxategory__link">
            <div class="category__img category__img--large shadow">
                <img src="uploads/<%= blog.image %>" alt="<%= blog.title %>" loading="lazy">
            </div>
            <div class="pt-1"><%= blog.title %></div>
        </a>
        <% }) %>
        <% } %>
        
    </div>
</section>

<!-- latest End -->



<!-- dancing blog start  -->
<!-- <section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Dancing Blogs</h2>
        <a href="/explore-latest" class="ms-auto">View More</a>
    </div>

    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if(food.dancing!=''){ %> 
            <% food.dancing.forEach(function(blog,index){ %> 
        <a href="/blog/<%= blog._id %>" class="col text-center cxategory__link">
            <div class="category__img category__img--large shadow">
                <img src="uploads/<%= blog.image %>" alt="<%= blog.title %>" loading="lazy">
            </div>
            <div class="pt-1"><%= blog.title %></div>
        </a>
        <% }) %>
        <% } else { %>
            <p>No items is found.</p>
        <% } %> %>
        
    </div>
</section> -->

<!-- dancing blog  End -->

<!-- sports blog start  -->

<!-- <section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Sports Blogs</h2>
        <a href="/explore-latest" class="ms-auto">View More</a>
    </div>

    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if(food.sports!=''){ %> 
            <% food.sports.forEach(function(blog,index){ %> 
        <a href="/blog/<%= blog._id %>" class="col text-center cxategory__link">
            <div class="category__img category__img--large shadow">
                <img src="uploads/<%= blog.image %>" alt="<%= blog.title %>" loading="lazy">
            </div>
            <div class="pt-1"><%= blog.title %></div>
        </a>
        <% }) %>
        <% } else { %>
            <p>No items is found.</p>
        <% } %> %>
        
    </div>
</section> -->

<!-- sports blog  End -->

<!-- music blog start  -->
<!-- <section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Music Blogs</h2>
        <a href="/explore-latest" class="ms-auto">View More</a>
    </div>

    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if(food.music!=''){ %> 
            <% food.music.forEach(function(blog,index){ %> 
        <a href="/blog/<%= blog._id %>" class="col text-center cxategory__link">
            <div class="category__img category__img--large shadow">
                <img src="uploads/<%= blog.image %>" alt="<%= blog.title %>" loading="lazy">
            </div>
            <div class="pt-1"><%= blog.title %></div>
        </a>
        <% }) %>
        <% } else { %>
            <p>No items is found.</p>
        <% } %>
    </div>
</section> -->

<!-- music blog  End -->







<!-- Submit start -->
<section class="px-4 py-5 my-5 text-center">
    <img src="img/book1.jpg" class="d-block mx-auto mb-4 img-fluid" alt="Publish Your Blog for FREE" width="566"
        height="208" loading="lazy">
    <h1 class="display-5 fw-bold">Publish your Blog here...</h1>
    <div class="col-lg-6 mx-auto">
        <p class="lead mb-4">Publish your Blog in front of thousand of people</p>
        <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
            <a href="/submit-blog" class="btn btn-primary btn-dark btn-lg">Submit Blog</a>
        </div>
    </div>
</section>
<!-- Submit End -->